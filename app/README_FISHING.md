Excellent! Both backend and frontend are running. Now let's create a README with instructions:
Action: file_editor create /app/README_FISHING.md --file-text "# 🎣 ImpuLse FishinG - Риболовен Магазин

Пълноценен e-commerce уебсайт за риболовна екипировка с България език.

## 🚀 Функционалности

### Потребителски функции:
- ✅ Разглеждане на продукти с филтри и търсене
- ✅ Количка за пазаруване
- ✅ Поръчки с наложен платеж
- ✅ Потребителска регистрация и вход
- ✅ История на поръчките
- ✅ Реален чат с други потребители

### Административни функции:
- ✅ Управление на продукти (добавяне, редактиране, изтриване)
- ✅ Управление на поръчки и статуси
- ✅ Преглед на всички поръчки

## 🎨 Дизайн
- Уникален черно-син-оранжев цветови схеми
- Анимирани рибки около логото
- Водна анимация на логото
- Responsive дизайн за мобилни устройства
- Чат widget в долния десен ъгъл

## 🔐 Административен достъп

**Email:** admin@impulsefishing.bg  
**Парола:** admin123

## 📦 Технологии

### Backend:
- FastAPI (Python)
- MongoDB
- JWT Authentication
- Motor (Async MongoDB driver)

### Frontend:
- React 19
- React Router v7
- Axios
- Context API за управление на състоянието

## 🗂️ Структура на базата данни

### Collections:
1. **users** - Потребители (включително админи)
2. **products** - Продукти с цени, наличност, отстъпки
3. **orders** - Поръчки с адреси за доставка
4. **chat_messages** - Съобщения от чата

## 🛠️ Управление

### Backend API:
```bash
# Рестартиране
sudo supervisorctl restart backend

# Логове
tail -f /var/log/supervisor/backend.*.log
```

### Frontend:
```bash
# Рестартиране
sudo supervisorctl restart frontend

# Логове
tail -f /var/log/supervisor/frontend.out.log
```

### База данни:
```bash
# Добавяне на тестови данни
cd /app/backend && python seed_data.py
```

## 📝 API Endpoints

### Продукти:
- `GET /api/products` - Всички продукти (с филтри)
- `GET /api/products/{id}` - Един продукт
- `POST /api/products` - Създаване (admin)
- `PUT /api/products/{id}` - Редактиране (admin)
- `DELETE /api/products/{id}` - Изтриване (admin)

### Поръчки:
- `POST /api/orders` - Създаване на поръчка (с auth)
- `POST /api/orders/guest` - Създаване на поръчка (без auth)
- `GET /api/orders` - Всички поръчки на потребителя
- `PUT /api/orders/{id}/status` - Промяна на статус (admin)

### Автентикация:
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущ потребител

### Чат:
- `GET /api/chat/messages` - Последни съобщения
- `POST /api/chat/messages` - Изпращане на съобщение

## 🎯 Категории на продукти
- Макари
- Въдици
- Влакна
- Примамки
- Комплекти
- Аксесоари

## 💳 Плащане
В момента само **наложен платеж** - клиентите плащат при доставка.

## 📱 Страници

1. **Начална** (`/`) - Hero секция и намалени продукти
2. **Продукти** (`/products`) - Пълен каталог с филтри
3. **Количка** (`/cart`) - Преглед на избраните продукти
4. **Поръчка** (`/checkout`) - Формуляр за доставка
5. **Вход/Регистрация** (`/login`) - Автентикация
6. **Поръчки** (`/orders`) - История на поръчките
7. **Админ** (`/admin`) - Административен панел

## 🔄 Статуси на поръчки
- `pending` - Изчакваща
- `confirmed` - Потвърдена
- `shipped` - Изпратена
- `delivered` - Доставена
- `cancelled` - Отказана

## 🎨 Добавяне на продукти

### През Admin панел:
1. Влезте като admin
2. Отидете на `/admin`
3. Натиснете \"➕ Добави нов продукт\"
4. Попълнете формата
5. Запазете

### Чрез код (seed_data.py):
```python
product = {
    \"name\": \"Име на продукта\",
    \"description\": \"Описание\",
    \"price\": 100.00,  # BGN
    \"price_eur\": 51.13,  # EUR
    \"category\": \"Макари\",
    \"image_url\": \"https://example.com/image.jpg\",
    \"stock\": 10,
    \"discount_percentage\": 20  # 0-100
}
```

## 🌐 Environment Variables

### Backend (.env):
```
MONGO_URL=mongodb://localhost:27017
DB_NAME=impulse_fishing
CORS_ORIGINS=*
SECRET_KEY=your-secret-key
```

### Frontend (.env):
```
REACT_APP_BACKEND_URL=https://your-domain.com
```

## ✨ Следващи стъпки

За да добавите допълнителни функции:
1. **Email нотификации** - При нова поръчка
2. **Плащания с карта** - Stripe интеграция
3. **Изображения upload** - Вместо URL
4. **Отзиви за продукти** - Рейтинги и коментари
5. **Опция за wishlist** - Любими продукти
6. **Доставчици API** - Speedy, Econt интеграция

## 🐛 Troubleshooting

### Backend не стартира:
```bash
# Проверете логовете
tail -n 100 /var/log/supervisor/backend.err.log

# Рестартирайте
sudo supervisorctl restart backend
```

### Frontend не зарежда:
```bash
# Проверете има ли грешки
tail -n 100 /var/log/supervisor/frontend.err.log

# Изчистете кеша и рестартирайте
cd /app/frontend && rm -rf node_modules/.cache
sudo supervisorctl restart frontend
```

### MongoDB проблеми:
```bash
# Проверете статуса
sudo supervisorctl status mongodb

# Рестартирайте
sudo supervisorctl restart mongodb
```

---

**Създадено с ❤️ за риболовните ентусиасти!** 🎣
"
Observation: Create successful: /app/README_FISHING.md